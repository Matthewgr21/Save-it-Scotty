# HTTP and File operations
requests>=2.31.0

# File and archive handling
pyzipper>=0.3.6

# Configuration (optional, for config.yaml support)
PyYAML>=6.0.1

# Windows-specific operations
pywin32>=306

# Build tools (only needed for creating executable)
pyinstaller>=6.3.0
